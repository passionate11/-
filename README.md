# 松一下

松一下是一个轻量 macOS 菜单栏休息提醒工具，覆盖眼睛远眺、站立活动和番茄休息。

## 功能

- 眼睛休息提醒：默认 20-20-20，使用电脑 20 分钟后，提醒眺望 6 米外至少 20 秒。
- 站立提醒：默认每 2 小时提醒一次，站立 20 分钟，并支持均衡活动、肩颈舒展、走动循环、恢复放松四套动作组合、阶段进度和轻柔/标准/活动一点三档强度。
- 眼睛提醒和站立提醒分别独立计时。
- 支持番茄钟预设：25 分钟专注，5 分钟休息。
- 设置窗口支持直接输入分钟和秒。
- 支持快速配置：均衡护眼、番茄专注、久坐打断和调试 10 秒可一键应用到眼睛、站立、风格和提醒窗口。
- 菜单栏支持快速节奏，可一键切换 20-20-20、番茄 25/5 和调试 10 秒。
- 菜单栏可实时显示眼睛提醒和站立提醒，也支持智能轮换的短文案模式。
- 站立完成后会在菜单栏短暂显示完成反馈，方便确认本次流程已经结束。
- 菜单栏会显示最近一次睡眠、锁屏、解锁或屏幕变化后的恢复诊断，并跨应用重启保留最近事件链。
- 菜单栏支持午休恢复压测，可模拟长时间离开后站立休息过期并验证自动结算。
- 菜单栏支持显示恢复压测，可模拟外接屏断开或多显示器切换后休息页跑到屏幕外的场景。
- 菜单栏支持设置窗口恢复压测，可模拟设置页落在旧屏幕外并验证自动回到当前可见屏幕。
- 菜单栏可复制显示环境诊断，记录当前屏幕组合、上次屏幕变化前后摘要、休息页窗口、设置窗口和窗口层级。
- 菜单栏可复制路线图状态，把 v0.1.45-0.1.47 的完成证据、当前运行状态和下一步检查整理成不打扰工作的文字快照。
- 菜单栏可运行显示变化追踪自检，不用真实拔插显示器也能验证屏幕变化记录链路。
- 菜单栏可运行真实显示环境自检，记录当前屏幕组合并验证休息页在真实显示器上贴合与重排。
- 支持暂停、立即眼睛休息、立即站立、重新开始全部计时。
- 支持独立设置窗口，可详细配置两个提醒、通知、提醒窗口和置顶强提醒。
- 设置窗口采用更接近 macOS System Settings 的宽面板左侧导航布局。
- 设置窗口默认打开 `今日概览`，用顶部状态带和双计时卡片展示眼睛/站立下一次提醒、今日完成数据、当前模式和下一步建议，并可直接开始眼睛休息、站立、暂停 30 分钟、复制问题反馈包或打开快速配置。
- 眼睛休息和站立提醒设置页也带有顶部摘要，能先看当前节奏、动作组合和强度，再细调分钟/秒。
- 设置页侧栏带有应用徽章、紧凑节奏摘要、底部版本信息和更轻的 macOS 列表导航；右侧页面标题带原生图标徽章与主题强调线，主卡片使用浅阴影与轻量分区，优先保证清爽可读。
- 设置窗口分栏继续精修：侧栏宽度、右侧内容区、底部操作区和概览快捷操作条重新对齐，透明点击层保留可点性，自绘轻按钮底减少旧式按钮感。
- 到点可弹出休息页，并发送系统通知；默认不强制置顶，点击卡片外背景或切到其他 App 时会让开，需要严格打断时可开启置顶强提醒。
- 支持多种整体风格：极简呼吸、松弛森林、像素窗边、软糖玩具、夜间护眼。
- 风格会同时影响设置窗口和全屏休息页。
- 显示设置页支持风格预览，切换风格时可以先看到休息页的大致气质；设置页主卡片也会跟随风格呈现轻量图案细节。
- 支持自动轻打扰：会议、演示、视频或游戏类应用在前台时，不弹全屏休息页，只保留系统通知和计时。
- 支持日历会议降打扰：授权后，当前日历事件进行中时自动只发通知，不弹全屏休息页。
- 支持日程关键词策略：录制、直播、面试等日程可自动暂停计时，普通会议继续只发通知。
- 菜单栏可复制真实日历诊断，查看当前授权、进行中的日程和实际命中的日历策略。
- 菜单栏可运行真实日历联动自检，在当前真实会议或日程中验证到点提醒是否按策略不弹休息页。
- 支持全屏/演示降打扰：进入全屏、隐藏 Dock 或隐藏菜单栏等演示状态时自动只发通知。
- 菜单栏可运行真实演示联动自检，在当前真实全屏/演示状态中验证到点提醒是否只通知、不弹休息页。
- 支持安静时段：用户可设置固定时间段自动只发通知、不弹全屏休息页，用公开能力覆盖固定勿扰/专注场景。
- 支持外部自动化链接：快捷指令、Raycast、Hammerspoon 等工具可打开 `songyixia://focus/on`、`songyixia://rhythm/debug` 等链接来联动专注/勿扰和快速节奏。
- 菜单栏可复制专注联动脚本，直接生成快捷指令、Hammerspoon 和 Raycast 可用的轻打扰模板。
- 菜单栏可复制自动化诊断，方便排查快捷指令、Raycast 或 Hammerspoon 联动问题。
- 自动化设置页会直接显示当前最终动作、命中原因、最近一次策略动作和建议下一步，帮助判断这次会正常弹窗、只发通知还是自动暂停。
- 支持自定义轻打扰应用白名单。
- 支持按应用和日程设置自动化策略：只发通知、自动暂停、或不处理。
- 支持菜单栏显示模式：眼睛 + 站立、只显示眼睛、只显示站立、极简图标、智能轮换。
- 支持登录时自动启动。
- 设置窗口包含本机 7 天休息趋势、近 30 天摘要、30 天热力图、跳过率、站立总时长、连续天数、自动化策略占比、CSV 导出、JSON 备份和恢复。
- 菜单栏支持查看关于信息，显示版本、构建号、安装位置、GitHub 源码入口和下载页，并可复制版本信息。
- 菜单栏支持检查更新，可检查 GitHub 最新 Release 并打开下载页。
- 菜单栏支持排查中心，把应用、恢复、显示、自动化、日历诊断和各类压测入口收在一个子菜单里。
- 菜单栏支持复制安装更新说明，可把下载、覆盖安装、确认版本和反馈问题步骤一次性复制出来。
- 菜单栏支持复制问题反馈包，生成适合直接粘贴到 GitHub Issue 的模板，并自动附带恢复问题报告和完整排查包。
- 菜单栏支持复制应用诊断，方便反馈计时、窗口、自动化和恢复状态。
- 菜单栏支持复制恢复场景矩阵，把锁屏、睡眠、午休、长离开、外接屏和窗口层级等场景整理成检查清单。
- 菜单栏支持复制恢复问题报告，把矩阵记录汇总成结论、待补场景和下一步建议。
- 菜单栏支持运行恢复矩阵套件，顺序触发关键恢复压测，减少手动逐项点击。
- 菜单栏支持复制完整排查包，一次性收集应用、恢复、显示、自动化和日历诊断。
- 本机诊断脚本会输出显示恢复摘要，包含显示诊断、设置窗口恢复、显示变化追踪和真实显示自检入口。
- 菜单栏支持反馈问题，可打开 GitHub Issue 并预填版本、系统、安装路径、下载页和反馈包链接。
- 设置会自动保存到 `UserDefaults`。

## 构建

```bash
scripts/build_app.sh
```

构建产物会生成到：

```text
outputs/EyeRest.app
```

GitHub Actions 会在 `main` 和 Pull Request 上运行 macOS 构建检查，覆盖 shell 语法、App bundle 生成、URL Scheme 和关键入口 selector。

构建时会自动生成 `AppIcon.icns` 并写入 App bundle。

本地构建会默认使用 macOS ad-hoc 签名，方便安装和打包后做完整性校验，不需要 Apple Developer 账号。需要跳过签名时可以运行 `SKIP_CODESIGN=1 scripts/build_app.sh`；需要使用正式证书时可以运行 `CODESIGN_IDENTITY="Developer ID Application: ..." scripts/build_app.sh`。

## 打包

```bash
scripts/package_app.sh
```

打包产物会生成到：

```text
dist/songyixia-<version>-<build>.zip
dist/songyixia-<version>-<build>.zip.sha256
```

解压后得到 `松一下.app`。

GitHub Actions 也会上传同名构建 artifact，方便直接下载测试。

应用版本号来自仓库根目录的 `VERSION` 文件，构建号可通过 `BUILD_NUMBER` 环境变量覆盖。

打包前会重新构建并签名，CI 会验证 zip 解压后的 `松一下.app` 仍然通过 `codesign --verify --deep --strict`，并校验同名 `.sha256` 文件与 zip 内容一致。

版本更新内容见 [CHANGELOG.md](CHANGELOG.md)。

## 发布到 GitHub

确认 `VERSION` 和 [CHANGELOG.md](CHANGELOG.md) 已更新后，先跑：

```bash
scripts/preflight_release.sh
```

再推送版本标签：

```bash
git tag v$(cat VERSION)
git push origin v$(cat VERSION)
```

GitHub Actions 会在 `v*` 标签上再次运行发布前检查，生成面向用户的 Release Notes，并把 `dist/songyixia-<version>-<build>.zip` 和同名 `.sha256` 上传到 GitHub Releases。标签版本必须和 `VERSION` 完全一致。

## 运行

```bash
open outputs/EyeRest.app
```

打开后点击菜单栏里的眼睛图标，选择 `打开设置...` 可以进入详细设置窗口。

菜单栏里的 `关于 松一下...` 会显示当前版本、构建号、安装位置、GitHub 仓库入口和最新下载页，也可以直接复制版本信息。

菜单栏里的 `检查更新...` 会检查 GitHub 最新 Release；发现新版时会优先识别 `songyixia-*.zip` 下载资源并提供直接下载，找不到资源时再打开发布页。

菜单栏里的 `复制分发维护方案` 会把当前版本、安装位置、Release 发布方式、正式签名/公证方案和自动更新评估复制出来，方便做发布决策或留存维护记录。

菜单栏里的 `复制自动更新评估` 会把当前手动 GitHub Release 更新链路、Sparkle 准备度、Developer ID/公证前置条件和推荐下一步复制出来。当前不会启用自动后台替换。

菜单栏里的 `排查中心` 会收纳应用、恢复、显示、自动化、日历诊断和各类压测入口。遇到锁屏、睡眠、午休、外接屏或窗口层级问题时，可以先用 `复制恢复场景矩阵` 看每个场景对应的压测入口和最近是否跑过，用 `复制恢复问题报告` 查看结论和下一步建议，也可以用 `运行恢复矩阵套件` 顺序跑一轮关键恢复压测；遇到复杂问题时直接用 `复制问题反馈包`，它会生成适合粘贴到 GitHub Issue 的模板，并附带恢复问题报告和完整排查包。

菜单栏里的 `排查中心` -> `复制安装更新说明` 会把当前版本、安装位置、下载页和 4 步安装/反馈流程复制到剪贴板，适合发给别人或自己留存。

菜单栏里的 `快速配置...` 可以一次性应用完整节奏：`均衡护眼`、`番茄专注`、`久坐打断` 或 `调试 10 秒`。设置窗口首次打开时也会提示一次，之后不再重复弹出。

菜单栏里的 `快速节奏` 可以直接切换 `20-20-20`、`番茄 25/5` 和 `调试 10 秒`。当前命中的节奏会有对勾，切换后会保存设置并重新开始眼睛休息计时。

菜单栏里的 `复制专注联动脚本` 会把常用 `songyixia://` 链接、快捷指令说明、Hammerspoon 示例和 Raycast Script Command 示例复制到剪贴板。

菜单栏里的 `复制自动化诊断` 会把 Bundle ID、安装位置、URL Scheme、常用联动链接、轻打扰状态、前台应用和策略数量复制到剪贴板。

菜单栏里的 `反馈问题...` 会打开 GitHub Issue，并预填当前版本、系统、安装路径、下载页和反馈包链接。遇到卡住、置顶、计时或自动化异常时，先在 `排查中心` 点 `复制问题反馈包`，再把诊断粘贴到 Issue 里。

普通用户更新链路：

1. 打开 [GitHub Releases](https://github.com/passionate11/-/releases/latest) 下载最新 `songyixia-<version>-<build>.zip`。
2. 解压得到 `松一下.app`，拖入 `/Applications` 覆盖旧版本。
3. 打开菜单栏 `关于 松一下...` 确认版本，或用 `检查更新...` 查看 GitHub 最新版本；有新版时可直接打开 zip 下载。
4. 反馈问题时先复制问题反馈包，再点 `反馈问题...` 打开预填好的 Issue。

菜单栏里的 `置顶强提醒` 默认关闭。关闭时，休息窗口保持普通层级，不会长期压在其他 App 前面；点击休息页卡片空白、背景，或切到其他 App 时会让它隐藏。打开后，休息页会用更强的置顶层级提醒你离屏。

如果 macOS 提示来自未验证开发者，可以在 Finder 中右键打开，或到系统设置里允许打开。

## 安装到应用程序

```bash
scripts/install_app.sh
```

脚本会先构建，再覆盖安装到：

```text
/Applications/松一下.app
```

## 冒烟测试

```bash
scripts/smoke_test.sh
```

脚本会构建并安装应用，然后检查 URL Scheme、单实例、设置窗口唤起、外部自动化链接和恢复压测；运行前会备份本机偏好，结束时自动恢复。

## 发布前检查

```bash
scripts/preflight_release.sh
```

脚本会构建、签名、诊断、检查版本和 changelog、打包 zip、解包后再次校验签名与包内容。GitHub Actions 也使用同一套检查，避免本地发布和 CI 校验不一致。

发布前检查还会生成 `dist/release-notes-<version>.md`，包含下载文件、SHA256、安装步骤、本次更新和反馈入口，避免 GitHub Release 正文直接塞完整 changelog。

## 发布就绪检查

```bash
scripts/release_readiness.sh
```

脚本只读地汇总版本、git 状态、发布 zip、SHA256 校验、已构建/已安装 App、签名、Gatekeeper、GitHub Release workflow 和自动更新当前方案；`scripts/release_readiness.sh --strict` 会在关键失败时返回非零，适合发版前留存一份不打扰工作的检查快照。

## 自动更新准备检查

```bash
scripts/auto_update_readiness.sh
```

脚本只读地检查当前更新链路是否仍适合保持手动 GitHub Release 模式，并列出接入 Sparkle 前需要补齐的条件：`Sparkle.framework`、`SUFeedURL`、`SUPublicEDKey`、appcast、ed25519 签名、Developer ID 签名、公证和回滚说明。`--strict` 只会在配置互相矛盾时失败，例如已经打包 Sparkle 但缺少 feed 或公钥。

## 路线图状态检查

```bash
scripts/roadmap_status.sh
```

脚本只读地检查当前 todo 证据，覆盖 v0.1.45 自动化体验、v0.1.46 设置页打磨和 v0.1.47 分发维护。App 菜单里的 `复制路线图状态` 和 `songyixia://diagnostics/roadmap-status` 会复制同类状态快照，并会进入问题反馈包和完整排查包。

## 自动化策略就绪检查

```bash
scripts/automation_policy_readiness.sh
```

脚本只读地检查自动化策略解释、设置页状态区、关键词编辑弹窗、问题反馈包和诊断文本是否都覆盖正常提醒、只发通知、自动暂停、不处理、日历和演示场景。它不会启动 App，也不会触发全屏休息页，适合少打扰地验证 v0.1.45 自动化体验。

## 公证准备检查

```bash
scripts/notarize_release.sh
```

脚本默认只做 dry-run，检查发布 zip、notarytool、解包后的 App、签名和 Gatekeeper 状态，不会联系 Apple。准备好 Developer ID 和 Apple 凭据后，可以用 `NOTARIZE_SUBMIT=1 APPLE_ID=... APPLE_TEAM_ID=... APPLE_APP_PASSWORD=... scripts/notarize_release.sh` 提交公证；当前 GitHub zip 流程仍可在没有这些凭据时正常发布。

## SwiftUI 迁移准备检查

```bash
scripts/swiftui_migration_readiness.sh
```

脚本只读地比较当前 Objective-C/AppKit 版本和 `Sources/EyeRest/main.swift` SwiftUI 草稿，列出双计时、设置页、恢复、自动化、统计、更新/反馈等迁移缺口。当前结论会明确标记为 `prototype only`，避免误把 SwiftUI 草稿当成可替换的正式版本。

## 设置合约准备检查

```bash
scripts/settings_contract_readiness.sh
```

脚本只读地检查当前 Objective-C/AppKit 正式版的 UserDefaults 设置键、默认值注册、备份恢复和旧键迁移，并对比 `Sources/EyeRest/main.swift` SwiftUI 草稿的存储方式。当前 SwiftUI 草稿仍使用单个 `EyeRestSettings` JSON blob，尚未共享正式版 per-key schema，因此只能作为 prototype，不能直接替换正式版本。

## 设置页视觉就绪检查

```bash
scripts/settings_visual_readiness.sh
```

脚本只读地检查设置窗口的 944px 宽面板、左侧原生侧栏、右侧 648px 内容区、概览/眼睛/站立/显示/自动化/统计页的关键布局、主题降噪和文档记录。它不会启动 App，也不会弹出休息页，适合在少打扰的情况下守住设置页观感。

## 本机诊断

```bash
scripts/diagnose_app.sh
```

脚本会读取 `/Applications/松一下.app` 的 bundle 信息、签名状态、运行进程、偏好设置摘要、显示恢复摘要和可用的窗口列表，不会修改应用状态。遇到窗口置顶、休息页卡住、多个进程、安装、签名、外接屏或设置页找不到等异常时，可以先运行它并把输出贴出来。

也可以检查未安装的构建产物：

```bash
APP_TARGET=outputs/EyeRest.app scripts/diagnose_app.sh
```

## 外部自动化

安装后可以从菜单栏复制常用自动化链接，也可以在快捷指令或其他自动化工具里直接打开这些 URL：

```text
songyixia://focus/on
songyixia://focus/off
songyixia://focus/toggle
songyixia://settings
songyixia://settings/eye
songyixia://settings/stand
songyixia://setup/balanced
songyixia://setup/pomodoro
songyixia://setup/stand
songyixia://setup/debug
songyixia://rest/eye
songyixia://rest/stand
songyixia://rhythm/202020
songyixia://rhythm/pomodoro
songyixia://rhythm/debug
songyixia://pause/30m
songyixia://resume
songyixia://backup/import
songyixia://automation/focus-template
songyixia://automation/diagnostic
songyixia://diagnostics/recovery-stress
songyixia://diagnostics/lunch-recovery
songyixia://diagnostics/sleep-hidden-recovery
songyixia://diagnostics/long-away-recovery
songyixia://diagnostics/display-recovery
songyixia://diagnostics/settings-window
songyixia://diagnostics/display-bounds
songyixia://diagnostics/display-real
songyixia://diagnostics/recovery-matrix
songyixia://diagnostics/recovery-report
songyixia://diagnostics/issue-bundle
songyixia://diagnostics/recovery-matrix-suite
songyixia://diagnostics/support-bundle
songyixia://diagnostics/display-change-trace
songyixia://diagnostics/display-live
songyixia://diagnostics/overlay-yield
songyixia://diagnostics/window-layer
songyixia://diagnostics/automation-policy
songyixia://diagnostics/presentation-policy
songyixia://diagnostics/presentation-live
songyixia://diagnostics/calendar-policy
songyixia://diagnostics/calendar-real
songyixia://diagnostics/calendar-live
```

## 说明

当前可运行版本使用 Objective-C + AppKit 实现，以便在只有 Command Line Tools、没有完整 Xcode 的环境中构建。`Sources/EyeRest/main.swift` 保留了 Swift/SwiftUI 版本草稿，后续安装完整 Xcode 后可以继续迁移成更现代的 SwiftUI 工程。

## 产品路线图

### 已完成

- 眼睛休息、站立提醒、番茄钟和自定义分钟/秒。
- 眼睛提醒和站立提醒独立计时，并避免多个休息窗口互相抢占。
- 全屏休息页，支持完成、稍后、跳过、延长。
- 休息窗口默认不强制置顶，可在设置页或菜单栏开启置顶强提醒。
- 默认休息页可让开：非置顶模式下点击卡片外背景或切到其他 App，会隐藏休息页并继续本轮休息计时。
- 多种整体风格：极简呼吸、松弛森林、像素窗边、软糖玩具、夜间护眼。
- 风格同时影响设置窗口和休息窗口。
- 熄屏/唤醒、屏幕睡眠、锁屏/解锁后自动结算过期休息，避免休息窗口卡住主屏幕。
- 休息窗口状态看门狗：自动修复窗口残留、窗口类型不一致、唤醒后窗口丢失或层级不对等状态。
- 恢复后连续复查：唤醒、解锁或屏幕变化后会在 1 秒、3 秒、8 秒自动复查休息页状态，减少系统刚恢复时漏校准。
- 恢复诊断：菜单栏记录最近一次睡眠、锁屏、解锁或屏幕变化后的处理结果，保留最近 20 条事件链并跨应用重启保留，可一键复制详细诊断文本，也可手动运行恢复自检。
- 一键冒烟测试：脚本可覆盖构建安装、单实例、设置窗口、外部自动化链接和恢复压测，方便每次迭代后快速确认关键路径。
- 暂停态清屏：暂停提醒时会主动关闭休息页和残留全屏窗口，避免暂停后仍挡住主屏幕。
- 今日轻量统计：眼睛完成、站立完成、稍后、跳过。
- 本机统计面板：展示最近一周休息完成趋势、近 30 天摘要、30 天热力图、跳过率、站立总时长、连续天数、自动化策略占比、CSV 导出和 JSON 备份。
- 多策略统计：统计手动完成、只发通知、自动暂停会话和自动暂停时长，并写入 CSV/JSON 导出。
- 统计面板趋势解释：根据本周/上周变化、跳过率、站立时长和连续天数给出轻量反馈。
- 月度视图摘要：近 30 天热力图旁展示活跃率、日均完成次数和最佳日。
- 数据备份与恢复：可导出/恢复本地 JSON，包含当前设置、自动化关键词和近 30 天休息统计。
- 菜单栏显示模式：完整、只看眼睛、只看站立、极简图标、智能轮换。
- 菜单栏信息密度优化：智能轮换模式会平时短显示，休息中或临近提醒时展开倒计时。
- 快速配置：可一键应用均衡护眼、番茄专注、久坐打断和调试 10 秒四套完整节奏，并支持 `songyixia://setup/...` 自动化。
- 菜单栏快速节奏：可直接切换 20-20-20、番茄 25/5 和调试 10 秒，并显示当前命中的节奏。
- 工作模式/轻打扰模式：会议、演示或专注时可从菜单栏开启，只保留系统通知和计时，不弹全屏休息页。
- 自动轻打扰模式：前台应用命中会议、演示、视频或游戏白名单时，自动抑制全屏休息页。
- 日历会议联动：授权读取日历后，当前会议进行中会自动进入只发通知模式。
- 日程关键词联动：可分别配置日程通知关键词和日程暂停关键词，录制、直播、演讲、面试等场景会自动暂停计时。
- 全屏/演示联动：检测到系统全屏、隐藏 Dock 或隐藏菜单栏状态时，自动进入只发通知模式。
- 安静时段：自动化设置页可设置固定时间段，在该时间内自动只发通知、不弹全屏休息页。
- 外部自动化链接：注册 `songyixia://`，可由快捷指令或其他工具触发轻打扰、快速节奏、暂停、立即休息、打开设置和恢复压测。
- 专注联动脚本模板：菜单栏可复制快捷指令、Hammerspoon 和 Raycast 示例，快速把系统专注或自定义热键接到轻打扰。
- 自动化诊断：可复制 Bundle ID、安装位置、自动化状态、常用联动链接、前台应用和策略数量，方便排查外部工具联动。
- 自动化策略结论：设置页、自动化诊断和问题反馈包会复用同一份最终动作、命中原因和建议下一步说明。
- 自动化设置页：可开关自动策略、查看当前前台应用/日程命中状态，并编辑/恢复应用和日程关键词。
- 自动化设置页精简：主页面保留状态、场景开关、安静时段和高级入口，应用/日程关键词移入独立编辑弹窗。
- 更细的自动化策略：按应用选择只发通知、自动暂停或不处理，按日程关键词选择只发通知或自动暂停；不处理优先级最高，适合修正误命中。
- 自动化关键词分组：关键词弹窗拆成应用策略和日程策略两组，并在顶部提示“不处理 > 自动暂停 > 只发通知”的优先级。
- 当前应用一键加入策略：关键词弹窗可把最近一个非松一下的前台应用追加到只通知、自动暂停或不处理列表，自动去重并优先使用 bundle id。
- 当前日程一键加入策略：当前有日历事件时，关键词弹窗可把会议标题追加到日程只通知或日程自动暂停列表。
- 自动化策略就绪检查：新增只读脚本守住策略解释、关键词优先级、诊断和反馈包证据，避免自动化体验退回到“看不懂为什么这么提醒”。
- 登录时自动启动。
- 休息页风格增强：每个风格有专属提示和更明显的背景细节。
- 休息页风格同源：全屏休息页、设置页和风格预览共用同一套图案语言，五种风格切换后不再像几套拼起来的界面。
- 休息页增加动作建议轮换：眼睛休息、站立提醒、番茄休息会给出不同建议。
- 休息页应急出口：全屏休息页可按 Esc 跳过当前提醒，菜单栏也提供“应急关闭休息页”来清理异常残留窗口并重启计时。
- 非置顶休息页让开：点击背景或卡片空白区域的第一下就会隐藏休息页，本轮休息计时继续；只有开启“置顶强提醒”时才会固定在最前面。
- 休息页按钮自检：完成、稍后、跳过和延长按钮支持窗口未激活时第一下点击，休息页展示或唤醒校准时会重新绑定按钮，发现按钮链路异常会自动重建休息页。
- 恢复压测：菜单栏可运行多轮恢复压测，连续校准休息页、按钮链路、残留窗口和屏幕挂载状态，并写入可复制的恢复诊断。
- 午休恢复压测：可模拟长时间离开后站立休息已经过期，验证自动结算、休息页关闭和下一轮站立计时重置。
- 睡眠隐藏恢复压测：可模拟睡眠/锁屏时休息页被隐藏但本轮休息未过期，验证该恢复时会恢复、已让开时不会异常弹回。
- 长离开恢复压测：可模拟睡眠/锁屏很久后眼睛和站立休息都已过期，验证两个提醒自动结算、休息页清理和下一轮计时重置。
- 显示恢复压测：可模拟休息页位于屏幕外，验证显示器切换或外接屏断开后会回到当前屏幕内。
- 设置窗口恢复压测：可模拟设置页位于屏幕外，验证显示器切换或外接屏断开后会回到当前可见屏幕内。
- 显示边界压测：可模拟休息页仍在屏幕内但尺寸继承旧显示器，验证会重新贴合当前屏幕并重排内容。
- 显示环境诊断：可复制当前屏幕组合、上次屏幕变化前后摘要、休息页窗口、设置窗口、窗口层级和轻打扰状态，方便反馈外接屏、投屏或合盖后的现场。
- 显示变化追踪自检：可模拟写入一条屏幕变化追踪记录，并验证显示诊断里的 `displayChangeFrom/displayChangeTo` 字段。
- 真实显示环境自检：可记录当前真实屏幕组合，并验证休息页贴合当前屏幕、内容重排和按钮链路。
- 窗口让开压测：可模拟非置顶休息页、设置页和用户切走场景，验证休息页让开后计时继续、设置页不被关闭。
- 窗口层级压测：可验证设置页保持普通窗口层级、普通休息页让开后不弹回，且只有“置顶强提醒”才使用高层级。
- 自动化策略压测：可模拟安静时段和自动暂停，验证只发通知、关闭休息页和顺延提醒时间。
- 演示策略压测：可模拟全屏/演示轻打扰，验证到点只发通知、已有休息页会关闭、恢复自检不会重新弹窗。
- 真实演示联动自检：当前系统处于真实全屏/演示状态时，可验证实际到点提醒是否只通知、不弹休息页。
- 日历策略压测：可模拟日历会议只通知和日程暂停，验证不弹休息页、暂停会关闭休息页并顺延提醒时间。
- 真实日历诊断：可复制当前授权、真实进行中日程、关键词命中和最终策略判断。
- 真实日历联动自检：授权且当前有真实日程时，可验证实际到点提醒是否按真实策略只通知或自动暂停。
- 本机诊断脚本：一键输出安装包、签名、进程、窗口和关键偏好状态，方便排查置顶、卡住、多实例和安装异常。
- 诊断脚本显示恢复摘要：输出显示环境诊断、显示变化追踪、设置窗口恢复和真实显示自检入口，并标记恢复历史里是否已有相关记录。
- 发布前检查脚本：统一本地和 CI 的构建、签名、打包、版本、变更记录和包体校验。
- GitHub Release 自动化：推送 `v*` 标签后自动运行发布前检查，并上传 macOS zip 到 Releases。
- 关于窗口：菜单栏可查看版本、构建号、安装位置、下载页和 GitHub 源码入口，并可复制版本信息。
- 检查更新：菜单栏可检查最新 GitHub Release，并在发现新版时提示下载。
- 排查中心：菜单栏用一个子菜单收纳应用、恢复、显示、自动化、日历诊断和各类压测入口。
- 应用诊断：菜单栏可复制完整状态摘要，方便反馈问题。
- 安装更新说明：菜单栏可复制当前版本、下载页、覆盖安装和反馈步骤。
- 恢复场景矩阵：可复制锁屏/睡眠、午休、长离开、外接屏、设置窗口、窗口让开和窗口层级等场景的压测入口、验证证据和最近运行状态。
- 恢复问题报告：可把恢复矩阵汇总成当前状态、覆盖结论、待补场景和建议下一步，适合直接反馈问题。
- 恢复矩阵套件：可顺序触发恢复场景矩阵中的关键压测，一轮覆盖常见卡主屏、按钮失效、外接屏和窗口层级问题。
- 完整排查包：可一次性复制应用诊断、恢复诊断、显示环境诊断、自动化诊断和日历诊断，适合反馈复杂问题。
- 问题反馈包：可生成适合直接贴到 GitHub Issue 的问题模板，并附带恢复问题报告和完整排查包。
- 问题反馈：菜单栏可打开 GitHub Issue，并自动带上版本、系统、安装路径、下载页和反馈包链接。
- 站立流程增强：可选择均衡活动、肩颈舒展、走动循环、恢复放松四套动作组合，全屏休息页会跟随组合切换阶段文案。
- 站立阶段面板：站立休息页会显示当前阶段、当前动作、下一步动作和阶段内进度，比单纯倒计时更容易跟着做。
- 自定义站立阶段：站立设置里可以编辑多行动作阶段，留空时继续使用内置动作组合。
- 站立强度偏好：支持轻柔、标准、活动一点三档强度，休息页动作建议和完成反馈会跟随强度变化。
- 站立完成反馈：完成站立后菜单栏会短暂展示刚完成的动作组合和站立时长。
- 站立完成建议：完成站立后会按动作组合和强度给出一条短建议，帮助用户更自然地回到工作节奏。
- 设置页视觉重构：从顶部分段切换改为左侧导航 + 右侧内容区。
- 设置页宽面板打磨：设置窗口、右侧内容区、主卡片和底部操作区重新排布，减少旧版小窗口里的拥挤和空白。
- 设置页侧栏选中态：左侧导航使用无边框列表行和主题色选中背景，更接近 macOS 原生设置面板。
- 设置页今日概览：默认首页展示眼睛/站立双计时、今日统计、当前模式和轻量建议，并在窗口可见时实时刷新。
- 概览页快捷操作：可直接开始眼睛休息、开始站立、暂停 30 分钟或复制问题反馈包，按钮会跟随开关和暂停状态自动启用/禁用。
- 概览页仪表盘打磨：顶部状态带显示下一次提醒、暂停、轻打扰或休息中状态，双计时卡片和快捷操作条重新排布，避免底部文字与按钮挤在一起。
- 眼睛/站立设置页摘要带：二级页面先显示当前节奏、休息时长、动作组合和强度，再进入具体分钟/秒和模式设置。
- 设置页底部操作区：应用和恢复默认放入更接近 macOS 设置的独立 footer，并跟随当前风格换色。
- 设置页增加原生列表行层级：眼睛、站立和显示方式页面使用行背景与分隔线组织控件。
- 设置页风格图案层：像素、玩具、森林、夜间等主题会在设置页主卡片里呈现不同图案细节，同时保持文字可读性。
- 显示方式页增加实时风格预览，预览画面和设置页图案语言保持一致。
- 设置页侧栏精修：左侧节奏摘要独立成卡片，导航行弱化按钮边框，版本信息沉到底部，更接近原生设置侧栏。
- 设置页主题降噪：主卡片减少大面积图案层，像素/玩具只保留轻量点缀，避免主题装饰压过设置内容。
- 设置页标题图标：右侧每个设置页都有跟随主题色的原生图标徽章，让二级页面更像同一套 macOS 小工具。
- 设置页品牌化标题：左侧增加应用徽章，导航改成透明点击层加自绘图标文字，右侧标题增加主题强调线，减少按钮框和工程面板感。
- 设置页分栏精修：设置窗口扩大到 944px，右侧内容区加宽，侧栏分隔线、节奏摘要阴影、列表选中态和概览快捷操作条统一成更轻的 macOS 设置面板风格。
- 设置页视觉降噪：整体背景、卡片阴影、行底色和主题图案透明度继续调轻，保留风格差异但让设置项优先清晰可读。
- 设置页视觉再设计：左侧改成更干净的品牌侧栏和透明导航，右侧内容区扩大到 648px，概览、眼睛、站立、显示、自动化和统计页重新拉开间距，并新增静态视觉就绪检查。
- 产品化反馈闭环：关于窗口、检查更新、下载页、问题反馈和问题反馈包串成一条普通用户能走完的链路。
- 更新资源直达：检查更新会解析 GitHub Release assets，优先打开 `songyixia-*.zip` 的直接下载链接，减少用户在发布页里找文件。
- 分发维护方案：菜单栏可复制当前 Release 发布方式、安装状态、正式签名/公证计划和自动更新评估，让 v0.1.47 的长期维护路径更明确。
- 自动更新准备检查：新增只读脚本和菜单复制项，明确当前仍走手动 GitHub Release 更新，并列出未来接 Sparkle 前必须补齐的签名、公证、appcast 和回滚条件。
- 路线图状态：菜单栏可复制 v0.1.45-0.1.47 的完成证据、当前运行状态和推荐下一步；脚本版 `scripts/roadmap_status.sh` 可在不启动 App 的情况下做同类检查。
- 发布就绪检查：新增只读脚本汇总版本、git/tag、zip、App bundle、签名、Gatekeeper、Release workflow 和自动更新方案，CI 发布前也会跑 strict 检查。
- 公证准备检查：新增 dry-run 公证脚本，先验证 zip、notarytool、签名和 Gatekeeper；有 Developer ID 与 Apple 凭据后可显式提交 notarytool。
- 发布包校验：打包时生成 `songyixia-*.zip.sha256`，发布前检查和发布就绪检查都会验证 checksum，GitHub artifact 和 Release 一起上传。
- 发布说明生成：新增 Release Notes 脚本，把下载文件、SHA256、安装步骤、本次更新和反馈入口整理成用户可读的 GitHub Release 正文。
- SwiftUI 迁移准备：新增只读迁移检查脚本，明确 SwiftUI 草稿仍是 prototype，并列出正式切换前必须补齐的功能缺口。
- 设置合约准备：新增只读脚本检查正式版 UserDefaults per-key schema 和 SwiftUI 草稿 JSON 存储的差距，避免未来迁移导致设置丢失。

### 下一批优先级

#### v0.1.45 自动化真实体验补强

- 计划：继续把自动化页从“能诊断”推进到“更容易配置”，下一步重点看默认关键词是否足够合理，以及是否需要为常见会议/视频/游戏场景提供更明确的推荐模板。
- 验收：设置页和问题反馈包都能直接回答“为什么这次只通知/自动暂停/正常弹窗”；关键词编辑弹窗能说明优先级并保留默认恢复；`automation_policy_readiness.sh --strict` 通过。
- 验证：优先静态脚本、`diagnose_app.sh` 和单项策略文本检查，不跑全屏休息冒烟。

#### v0.1.46 设置页继续打磨

- 计划：继续收敛二级页控件密度，重点处理按钮文案、表单行节奏、夜间/像素/玩具风格的文字对比和风格预览细节。
- 验收：设置页不再像调试面板；左侧侧栏、右侧标题、主卡片、footer 和弹窗的按钮层级一致；`settings_visual_readiness.sh --strict` 通过。
- 验证：静态编译、视觉就绪脚本和必要的窗口级截图，避免触发全屏休息页。

#### v0.1.47 分发和长期维护

- 计划：把 Release 下载、覆盖安装、检查更新、反馈包、自动更新评估、签名/公证和 SwiftUI 迁移合并成一条维护清单。
- 验收：普通用户能从 GitHub Release 下载 zip、拖入 `/Applications`、确认版本、检查更新并复制反馈包；自动更新和公证仍保持明确的前置条件。
- 验证：`release_readiness.sh`、`auto_update_readiness.sh`、`settings_contract_readiness.sh` 和打包校验。

### 后续可以探索

- SwiftUI 版本：在完整 Xcode 环境下迁移到 SwiftUI，提高长期维护性。
