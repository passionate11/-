# Changelog

## main

- 菜单栏新增 `复制路线图状态`，同时新增 `scripts/roadmap_status.sh`，可只读汇总 v0.1.45-0.1.47 的完成证据、下一步检查和当前运行状态，并接入问题反馈包/完整排查包。
- 设置窗口继续精修：窗口加宽到 920px，侧栏、右侧内容和底部操作区重新对齐，概览快捷操作改为自绘轻按钮底，整体更接近 macOS 设置面板。
- 新增 `scripts/swiftui_migration_readiness.sh`，只读评估 SwiftUI 草稿和当前 Objective-C/AppKit 版本的功能差距，明确 SwiftUI 版本仍是 prototype，避免误切换。
- 新增 `scripts/generate_release_notes.sh`，发布前生成面向用户的 GitHub Release 正文，包含下载文件、SHA256、安装步骤、本次更新和反馈入口。
- 打包流程新增 `songyixia-*.zip.sha256`，发布前检查和发布就绪检查会验证 checksum，GitHub artifact 和 Release 会一起上传校验文件。
- `检查更新...` 会解析 GitHub Release assets，发现新版时优先提供 `songyixia-*.zip` 直接下载入口，找不到 zip 时再回退到发布页。
- 新增 `scripts/notarize_release.sh` 公证准备脚本，默认 dry-run 检查发布 zip、notarytool、签名和 Gatekeeper；设置 `NOTARIZE_SUBMIT=1` 并提供 Apple 凭据后可提交公证。
- 新增 `scripts/release_readiness.sh` 发布就绪检查，只读汇总版本、git/tag、zip、App bundle、签名、Gatekeeper、Release workflow 和自动更新当前方案，并接入发布前检查与 Release workflow。
- 菜单栏新增 `复制分发维护方案`，可整理当前 Release 发布方式、安装状态、正式签名/公证计划和自动更新评估，让 v0.1.47 的长期维护路线更清楚。
- 设置页右侧标题区新增跟随主题色的原生图标徽章和强调线，左侧增加应用徽章并把导航改成透明点击层，让概览、眼睛、站立、显示、自动化和统计页的视觉层级更统一。
- 设置窗口继续做视觉收敛：标题栏透明、侧栏增加产品副标题，导航行更像 macOS 列表，主卡片增加轻量阴影，减少调试面板感。
- 自动化设置页把策略结论整理成摘要区，新增最近一次策略动作，并修正自动暂停开始时记录原因可能读取旧状态的问题。
- 设置页侧栏继续收敛为更接近 macOS 设置的形态：左侧增加紧凑节奏摘要，导航行去掉明显按钮质感，版本信息固定在底部。
- 设置页主卡片降低大面积主题图案干扰，像素/玩具风只保留轻量点缀，让设置项优先保持清爽、可读和可点击。
- 自动化页新增策略结论，把当前最终动作、命中原因和建议下一步直接展示出来，减少“为什么这次只通知/自动暂停/正常弹窗”的猜测。
- 自动化诊断和问题反馈包新增同一份策略结论，反馈时会直接带上 `section=automation-policy`。

## 0.1.44

- 关于窗口增加下载页入口和复制版本信息，反馈问题时可以直接带上版本、系统、安装位置、下载页和反馈包链接。
- GitHub Issue 反馈模板补充下载页和 `songyixia://diagnostics/issue-bundle`，把版本检查、下载、诊断复制和反馈串成更完整的闭环。
- README 增加普通用户安装、更新和反馈链路说明，Release 下载、拖入 `/Applications`、检查更新和反馈问题更容易串起来。
- 发布前检查新增产品化闭环守卫，避免关于窗口和反馈模板退回只显示基础信息。

## 0.1.43

- 设置窗口加宽到更接近 macOS 设置面板的比例，右侧内容区、卡片和底部操作区重新排布，减少空白和拥挤感。
- 左侧导航改成无边框列表行，并新增随主题色变化的选中态，避免旧版灰色按钮感。
- 今日概览、站立提醒、显示方式、自动化和统计页同步拉开控件间距，双计时卡片、风格预览和策略状态更容易扫描。
- 发布前检查新增设置页侧栏视觉守卫，避免后续退回旧的纹理按钮导航。

## 0.1.42

- 站立休息页新增阶段面板，单独显示当前阶段、当前动作、下一步动作和阶段内进度。
- 站立页的动作提示从单行摘要升级为更明确的流程引导，眼睛休息和番茄休息继续保持轻量提示。
- 发布前检查新增站立阶段 UI 守卫，避免后续退回只有倒计时的站立提醒。

## 0.1.41

- 全屏休息页改用和设置页、风格预览一致的风格图案系统，极简、森林、像素、玩具、夜间五种风格的视觉语言更统一。
- 休息页卡片内部增加轻量同源图案层，并调整卡片透明度和阴影，让休息页更像完整设计而不是单张半透明卡片。
- 发布前检查新增休息页风格图案守卫，避免后续退回旧的单独装饰逻辑。

## 0.1.40

- 休息页的完成、稍后、跳过和延长按钮改成专用按钮，支持窗口未激活时第一下点击直接生效，降低睡眠/午休回来后“点了没反应”的概率。
- 发布前检查新增休息页按钮类型守卫，避免后续改动把第一下点击能力退回去。

## 0.1.39

- 眼睛休息页和站立提醒页新增顶部摘要带，直接显示当前节奏、休息时长、动作组合和站立强度。
- 眼睛/站立设置页重新排布为“摘要 + 原生列表行”，和今日概览的仪表盘层级更统一。
- 外部自动化新增 `songyixia://settings/eye` 和 `songyixia://settings/stand`，可直达对应设置页，也方便发布前截图验证。

## 0.1.38

- `今日概览` 改成更清晰的仪表盘布局：新增顶部状态带，直接显示下一次提醒、暂停、轻打扰或休息中状态。
- 眼睛和站立双计时卡片重新压缩排布，底部统计、建议和快捷操作不再互相挤占。
- 概览页快捷操作收进统一操作条，并优化夜间、玩具等风格下的文字对比和图案干扰。

## 0.1.37

- 新增 `快速配置...`，可一键应用均衡护眼、番茄专注、久坐打断和调试 10 秒四套完整节奏。
- `今日概览` 新增快速配置入口，首次打开设置时会提示一次，之后不再重复打扰。
- 外部自动化新增 `songyixia://setup/balanced`、`songyixia://setup/pomodoro`、`songyixia://setup/stand` 和 `songyixia://setup/debug`。

## 0.1.36

- 设置页主卡片增加随风格变化的轻量图案层，像素、玩具、森林、夜间等主题不再只是换背景色。
- 显示方式页的风格预览改用同一套图案语言，预览和设置页主体更一致。
- 修正森林风在设置页误用白色文字导致可读性过低的问题，夜间风继续保持暗色设置页。

## 0.1.35

- `今日概览` 增加快捷操作，可直接开始眼睛休息、开始站立、暂停 30 分钟或复制问题反馈包。
- 概览页快捷按钮会根据眼睛/站立开关和暂停状态自动启用或禁用，避免点了没有反馈。
- 设置窗口底部操作区改成更接近 macOS 设置的独立 footer，并跟随当前风格换色。

## 0.1.34

- 设置窗口新增 `今日概览` 首页，用两张大卡片同时展示眼睛和站立的独立倒计时、进度、当前模式和今日完成/稍后/跳过数据。
- 概览页会在设置窗口可见时随每秒计时刷新，休息中、暂停中、自动暂停和轻打扰状态都能直接看出来。
- 左侧摘要改成更紧凑的双行节奏摘要，设置页默认打开后更像一个状态仪表盘。

## 0.1.33

- 菜单栏新增 `排查中心` 子菜单，把应用、恢复、显示、自动化、日历诊断和各类压测入口收进一个统一入口，减少一级菜单拥挤。
- 新增 `复制问题反馈包` 和 `songyixia://diagnostics/issue-bundle`，会生成适合直接贴到 GitHub Issue 的模板，并附带恢复问题报告和完整排查包。
- `反馈问题...` 模板改为提示先复制问题反馈包，自动化链接菜单也修正了诊断项重复显示“复制”的文案。

## 0.1.32

- 菜单栏新增 `复制恢复问题报告`，把恢复矩阵记录汇总成更易读的结论、当前窗口状态、已覆盖场景、待补场景和下一步建议。
- 外部自动化新增 `songyixia://diagnostics/recovery-report`，可脚本化复制人话版问题报告，并在文本中保留 `recoveryReport=1` 和 `summary=` 机器标记。
- 完整排查包新增 `section=recovery-report`，自动化诊断和本机诊断脚本也会列出报告入口，方便从“日志排查”升级到“问题摘要”。

## 0.1.31

- 菜单栏新增 `运行恢复矩阵套件`，会顺序触发基础恢复、午休、睡眠隐藏、长离开、显示恢复、设置窗口、显示边界、窗口让开和窗口层级压测。
- 外部自动化新增 `songyixia://diagnostics/recovery-matrix-suite`，可一键跑完整恢复矩阵；重复触发时旧一轮后续步骤会自动失效。
- 恢复场景矩阵、完整排查包、自动化诊断和本机诊断脚本都会列出恢复矩阵套件入口，方便从“看清单”推进到“一键跑清单”。
- 恢复历史容量从 20 条扩到 80 条，确保一轮恢复矩阵套件的开始、过程和完成记录都能保留在诊断里。

## 0.1.30

- 菜单栏新增 `复制恢复场景矩阵`，把锁屏/睡眠、午休、长离开、外接屏、窗口让开和窗口层级等恢复场景整理成一份可复制清单。
- 外部自动化新增 `songyixia://diagnostics/recovery-matrix`，可脚本化复制场景、对应压测 URL、验证证据和最近是否跑过。
- 完整排查包新增 `section=recovery-matrix`，本机诊断脚本也会列出恢复矩阵入口，方便继续补真实场景测试。

## 0.1.29

- 菜单栏新增 `复制完整排查包`，把应用诊断、恢复诊断、显示环境诊断、自动化诊断和日历诊断合并到一个剪贴板文本。
- 外部自动化新增 `songyixia://diagnostics/support-bundle`，方便快捷指令、Raycast 或排查脚本一键收集完整现场。
- 完整排查包新增 `supportBundle=1` 标记，便于脚本和反馈模板确认用户粘贴的是聚合诊断。

## 0.1.28

- 本机诊断脚本新增 `Display Recovery` 段，直接列出显示环境诊断、显示变化追踪、设置窗口恢复和真实显示自检 URL。
- 诊断脚本会从恢复历史中标记是否已有屏幕变化、设置窗口恢复或真实显示自检记录，方便排查外接屏、投屏和合盖问题。
- 发布前检查会验证诊断脚本包含显示恢复信息，避免后续排查能力回退。

## 0.1.27

- 屏幕变化、外接屏断开或合盖后会同步检查设置窗口，发现设置页落在旧屏幕外时自动移回当前可见屏幕。
- 菜单栏新增 `运行设置窗口恢复压测`，外部自动化新增 `songyixia://diagnostics/settings-window`，可模拟设置窗口跑到屏幕外并验证恢复。
- 显示/恢复诊断会记录 `设置页回到屏幕内`，方便区分休息页恢复和设置窗口恢复。

## 0.1.26

- 显示环境诊断新增屏幕变化追踪，记录上一次屏幕变化的前后摘要，方便排查外接屏拔插、合盖、投屏和分辨率变化。
- 菜单栏新增 `运行显示变化追踪自检`，外部自动化新增 `songyixia://diagnostics/display-change-trace`，不用真实拔插显示器也能验证追踪链路。
- 恢复事件里的 `屏幕变化` 详情会附带变化前后屏幕摘要，和 `复制显示环境诊断` 的 `displayChangeFrom/displayChangeTo` 字段对应。

## 0.1.25

- 菜单栏新增 `复制显示环境诊断`，可一键复制当前屏幕组合、休息页窗口、设置窗口、窗口层级和轻打扰状态。
- 外部自动化新增 `songyixia://diagnostics/display-real`，适合外接屏、投屏、合盖或分辨率变化后快速收集现场信息。
- 自动化诊断新增显示环境诊断链接，区分“只复制诊断”的 `display-real` 和“弹出休息页自检”的 `display-live`。

## 0.1.24

- 菜单栏新增 `运行真实显示环境自检`，使用当前真实屏幕组合验证休息页是否在屏幕内、贴合当前屏幕并完成内容重排。
- 外部自动化新增 `songyixia://diagnostics/display-live`，可脚本化记录当前屏幕数量、主屏 frame、visibleFrame 和休息页窗口状态。
- 自检结束会还原眼睛/站立计时和休息页状态，方便排查外接屏、双屏和分辨率变化问题。

## 0.1.23

- 菜单栏新增 `运行真实演示联动自检`，在系统真实全屏/演示状态下验证到点提醒只发通知、不弹休息页。
- 外部自动化新增 `songyixia://diagnostics/presentation-live`，可脚本化触发真实演示/全屏自检；未检测到演示状态时会记录跳过原因。
- 自检会还原自动化开关、眼睛计时和今日统计，方便在 Keynote、会议软件或全屏视频中安全验证。

## 0.1.22

- 菜单栏新增 `运行真实日历联动自检`，在真实日历授权且当前有进行中事件时，验证到点提醒会按真实会议/日程策略只通知或自动暂停。
- 外部自动化新增 `songyixia://diagnostics/calendar-live`，可脚本化触发真实日历端到端自检；无授权或无当前事件时会明确记录跳过原因。
- 自检会还原自动化开关、眼睛计时和今日统计，避免真实日历测试污染日常数据。

## 0.1.21

- 菜单栏新增 `复制真实日历诊断`，可读取当前真实日历授权和进行中的日程，判断会走只发通知、自动暂停还是默认会议策略。
- 外部自动化新增 `songyixia://diagnostics/calendar-real`，方便脚本化复制真实日历诊断文本。
- 自动化诊断会附带真实日历诊断链接，后续排查会议/日程联动更直接。

## 0.1.20

- 菜单栏新增 `运行日历策略压测`，模拟日历会议只通知和日程暂停两条策略分支。
- 外部自动化新增 `songyixia://diagnostics/calendar-policy`，可脚本化验证会议中不弹休息页、日程暂停会关闭休息页并顺延提醒。
- 诊断压测会还原自动化开关、今日统计和休息计时，避免测试污染真实使用数据。
- 自动化策略压测改为固定模拟自动暂停命中，避免受当前前台应用切换影响而偶发失败。

## 0.1.19

- 菜单栏新增 `运行演示策略压测`，模拟全屏/演示轻打扰场景，验证到点时只发通知、不弹休息页。
- 外部自动化新增 `songyixia://diagnostics/presentation-policy`，可脚本化验证演示命中后已有休息页会关闭，恢复自检也不会把休息页拉回。
- 冒烟测试新增演示策略校验，继续覆盖会议/演示/全屏场景下不打断用户的核心路径。

## 0.1.18

- 修复设置页或普通休息页在应用失焦后仍像“固定在最前面”的体感问题：失焦时会把窗口层级校准回普通窗口并让设置页退到后面。
- 新增 `运行窗口层级压测` 和 `songyixia://diagnostics/window-layer`，验证只有开启“置顶强提醒”时休息页才会使用高层级。
- 诊断脚本改进窗口列表解析，方便直接看到松一下窗口的 layer、onscreen 和 bounds 状态。

## 0.1.17

- 菜单栏新增 `运行自动化策略压测`，验证安静时段会只发通知、不弹休息页。
- 外部自动化新增 `songyixia://diagnostics/automation-policy`，可脚本化验证自动暂停会关闭休息页并顺延下一次提醒。
- 冒烟测试新增自动化策略校验，防止会议/演示/安静时段相关策略回退成全屏打扰。

## 0.1.16

- 菜单栏新增 `运行睡眠隐藏恢复压测`，模拟睡眠/锁屏时休息页被隐藏但本轮休息还没过期的场景。
- 外部自动化新增 `songyixia://diagnostics/sleep-hidden-recovery`，可验证隐藏的未过期休息页会恢复、已让开的非置顶休息页不会异常弹回。
- 冒烟测试新增睡眠隐藏恢复校验，继续收敛熄屏、锁屏和长时间离开后的窗口恢复边界。

## 0.1.15

- 修复非置顶休息页第一下点击经常只激活应用、没有让开的体感问题；现在背景和卡片空白区域第一下点击就会隐藏休息页。
- 菜单栏新增 `运行显示边界压测`，模拟休息页仍在当前屏幕内但尺寸继承旧显示器的场景。
- 外部自动化新增 `songyixia://diagnostics/display-bounds`，可脚本化验证休息页会重新贴合当前屏幕并重排内容。
- 恢复逻辑新增窗口尺寸自检：休息页可见但 frame 不匹配当前屏幕时，会自动重新贴合。

## 0.1.14

- 菜单栏新增 `运行长离开恢复压测`，模拟睡眠/锁屏很久后眼睛和站立休息都已过期的场景。
- 外部自动化新增 `songyixia://diagnostics/long-away-recovery`，可脚本化验证长时间离开后两个提醒会自动结算并重新排期。
- 冒烟测试新增长离开恢复校验，确认过期休息页会清理干净，不再占住主屏幕。

## 0.1.13

- 菜单栏新增 `运行窗口让开压测`，模拟非置顶休息页、设置页和用户切走场景。
- 外部自动化新增 `songyixia://diagnostics/overlay-yield`，可脚本化验证休息页让开后计时继续、设置页不被关闭。
- 冒烟测试新增窗口让开压测校验，防止休息页重新变成长期压住主屏幕的状态。

## 0.1.12

- 修复非强提醒休息页仍像“固定在最前面”的问题：默认休息页保持普通层级，应用失焦时会主动让开。
- 非强提醒休息页点击卡片空白或背景都会隐藏，只保留完成、稍后、跳过和延长按钮响应点击。
- 休息页弹出时不再关闭设置窗口，窗口诊断新增 key/main 状态，便于排查层级问题。
- 菜单栏新增 `复制自动化诊断`，聚合 URL Scheme、常用联动链接、轻打扰状态、前台应用和策略数量。
- 外部自动化新增 `songyixia://automation/diagnostic`，可脚本化复制自动化排查信息。
- 冒烟测试新增自动化诊断剪贴板校验，方便排查快捷指令、Raycast 或 Hammerspoon 联动问题。

## 0.1.11

- `复制专注联动脚本` 新增 Raycast Script Command 示例，覆盖切换轻打扰和暂停 30 分钟。
- 冒烟测试新增 Raycast 模板校验，确认复制出的专注联动模板包含 Raycast 配置片段。

## 0.1.10

- 菜单栏新增 `复制专注联动脚本`，一键复制 macOS 快捷指令和 Hammerspoon 可用的轻打扰联动模板。
- 外部自动化新增 `songyixia://automation/focus-template`，可直接把专注联动模板复制到剪贴板。
- 冒烟测试新增剪贴板校验，确认模板包含 `songyixia://focus/on`、`songyixia://focus/off` 和 Hammerspoon 示例。

## 0.1.9

- 菜单栏新增 `运行显示恢复压测`，用于模拟休息页跑到屏幕外的显示器切换场景。
- 外部自动化新增 `songyixia://diagnostics/display-recovery`，可脚本化验证外接屏断开或多显示器切换后的窗口恢复。
- 冒烟测试新增显示恢复校验，确认屏幕外休息页会回到当前屏幕内并清理测试状态。

## 0.1.8

- 菜单栏新增 `运行午休恢复压测`，专门模拟长时间离开后站立休息已经过期的场景。
- 外部自动化新增 `songyixia://diagnostics/lunch-recovery`，可由脚本、快捷指令或测试流程触发午休恢复压测。
- 冒烟测试新增午休恢复校验，确认过期站立休息会自动结算、关闭休息页并重置下一次站立计时。

## 0.1.7

- 修复默认休息页像“固定在最前面”的问题：非置顶模式不再主动抢焦点，也不再跨所有桌面显示。
- 非置顶休息页点击卡片外背景或切到其他 App 时会自动让开，本轮休息计时继续，不会下一秒又弹回前台。
- 应用诊断新增休息页层级、窗口行为和“已让开”状态，方便排查窗口挡住主屏幕的问题。

## 0.1.6

- 菜单栏新增 `快速节奏`，可一键切换 `20-20-20`、`番茄 25/5` 和 `调试 10 秒`。
- 当前节奏会在快速节奏菜单里打勾，切换后会自动保存并重新开始眼睛休息计时。
- 外部自动化新增 `songyixia://rhythm/debug`、`songyixia://rhythm/pomodoro` 和 `songyixia://rhythm/202020`。

## 0.1.5

- 菜单栏新增 `反馈问题...`，可直接打开 GitHub Issue，并预填版本、系统和安装路径。
- Issue 模板会提示先复制应用诊断，方便反馈窗口置顶、卡住、计时或自动化问题。

## 0.1.4

- 休息窗口默认不再强制置顶，切到其他 App 后不会一直压在最前面。
- 设置页和菜单栏新增 `置顶强提醒`，需要严格打断时可手动开启。
- 应用诊断新增置顶强提醒状态，方便排查窗口层级问题。

## 0.1.3

- `检查更新...` 改为真实检查 GitHub 最新 Release，并提示是否已经是最新版本。
- 更新检查失败时提供打开下载页兜底。

## 0.1.2

- 菜单栏新增 `复制应用诊断`，可一键复制版本、计时、设置、自动化、窗口和恢复事件摘要。
- 方便反馈卡住、置顶、多实例、提醒异常等问题时直接附带状态信息。

## 0.1.1

- 菜单栏新增 `检查更新...`，可直接打开 GitHub 最新 Release 页面。
- 关于/更新入口更完整，方便已安装用户找到源码和新版下载包。

## 0.1.0

首个可运行的 macOS 菜单栏版本。

- 眼睛休息提醒：支持 20-20-20、番茄钟和自定义分钟/秒。
- 站立提醒：眼睛和站立分别独立计时，默认每 2 小时提醒一次，站立 20 分钟。
- 全屏休息页：支持完成、稍后、跳过、延长和 Esc 应急跳过。
- 多风格 UI：极简呼吸、松弛森林、像素窗边、软糖玩具、夜间护眼，风格同时影响设置页和休息页。
- 设置窗口：macOS 风格左侧导航，覆盖眼睛、站立、显示、自动化和统计。
- 自动化：支持轻打扰、会议/演示/视频/游戏识别、日历会议、安静时段和 `songyixia://` 外部链接。
- 统计与备份：本机 7 天趋势、30 天摘要、热力图、CSV 导出和 JSON 备份/恢复。
- 恢复诊断：支持睡眠/锁屏/唤醒后状态校准、恢复压测和本机诊断脚本。
- 分发：生成 AppIcon、本地 ad-hoc 签名、zip 打包、安装到 `/Applications/松一下.app`，并通过 GitHub Actions 构建校验。
